# Database Configuration
# PostgreSQL connection string (asyncpg driver)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/postgres

# API Source Configuration
# Optional: API key for external APIs (CoinPaprika/CoinGecko don't require keys for basic usage)
API_SOURCE_KEY=

# CSV Source Configuration
# Path to cryptocurrency CSV file
CSV_PATH=data/sample.csv

# Logging Configuration
# Log level: DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO

# Scheduler Token (Optional but Recommended)
# This token protects the /trigger-etl endpoint
# If set, requests to /trigger-etl must include header: X-Scheduler-Token: YOUR_TOKEN
# If not set, /trigger-etl endpoint is publicly accessible (not recommended for production)
# 
# To generate a secure token, run:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
# Or use: openssl rand -hex 32
#
SCHEDULER_TOKEN=
